---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: contentanalyst/java-bash

inputs:
  - name: git-repo
  - name: gradle-project

run:
  path: ./gradlew-project/gradlew
  args:
    - test
