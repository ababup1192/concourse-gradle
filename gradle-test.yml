---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: jtech/oracle-jdk

inputs:
  - name: gradle-project

run:
  path: bash
  args:
    - c
    - |
      cd gradle-project
      gradlew test
