---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: frekele/gradle

inputs:
  - name: git-repo
  - name: gradle-project

run:
  path: bash
  args:
    - -c
    - |
      cd gradle-project
      gradle test
