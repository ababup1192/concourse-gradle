---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: contentanalyst/java-bash

inputs:
  - name: git-repo
  - name: gradle-project

run:
  path: ./gradle-project/gradlew
  args:
    - test
